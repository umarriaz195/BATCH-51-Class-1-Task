// This program stores a person's name with whitespace characters at the beginning and end in a variable, prints the name with whitespace, and then prints the name again after stripping the whitespace.
var personName0 = "\t Umar\n";
console.log(personName0);
var trimmedName0 = personName0.trim();
console.log(trimmedName0);
