function makeShirt(size: string, message: string): void {
    console.log(`Shirt with size ${size.toUpperCase()} looks fit on you. ${message}`);
  }
  
   makeShirt("medium", "Always be in your zone!");
  
  